import type { Centro } from '../types/centro';

// Vacio a proposito: a diferencia de los centros en Colombia, estos vienen
// exclusivamente del scraper en vivo de redporvenezuela.com (no hay seeds a
// mano todavia), asi que no hay un ejemplo real verificado para usar como
// fallback sin inventar datos.
export const FALLBACK_CENTROS_VENEZUELA: Centro[] = [];
